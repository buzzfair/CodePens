//working out longest word function

let longestString = '';
function findLongestWord(wordList,word) {
//	for (let i = 0; i < wordList.length; i++) {
//	wordList[i].length;
	for (let word of array) {
		if (word.length > longestString) {
		longestString = word;
	} 
} return longestString;

